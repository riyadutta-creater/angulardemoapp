import { TransformDataPipePipe } from './transform-data-pipe.pipe';

describe('TransformDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
